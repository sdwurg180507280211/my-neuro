#ASR+LLM依赖

fastapi
uvicorn
python-multipart
# PyTorch 锁定 2.4.0 版本（经过测试兼容良好）
# conda-forge 仍为 macOS Intel (x86_64) 提供预编译包，安装命令：
#   conda install -n my-neuro -y pytorch=2.4.0 pytorch-cpu torchvision torchaudio -c conda-forge
torch==2.4.0
torchaudio==2.4.0
# numba 0.61.2 需要 numpy < 2.3.0，否则兼容性错误
numpy<2.3.0
funasr>=0.9.6
websockets
modelscope
onnxruntime
datasets
peft
py7zr

#屏幕点击工具依赖
openai
pillow
pyautogui

#RAG依赖
sentence-transformers
watchdog

#打包exe
PyQt5
requests
pyinstaller

#MemOS 记忆系统依赖
streamlit
aiohttp
pydantic>=2.0.0
scikit-learn

# 向量数据库
qdrant-client>=1.7.0

# 图数据库（可选）
neo4j>=5.0.0

# 检索增强
rank-bm25>=0.2.2

# 文档处理
pypdf>=3.0.0
beautifulsoup4>=4.12.0

# 轻量级图数据库（NetworkX 替代 Neo4j）
networkx>=3.0

# 知识图谱可视化（WebUI 使用）
pyvis>=0.3.0

# CLIP 图像理解（用于图片记忆的高级检索）
transformers>=4.30.0

markdown
